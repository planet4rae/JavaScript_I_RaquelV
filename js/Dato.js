const _ingresos = [100,500,1000];

const _totalIngreso = () => {
    let totalIngreso = 0;

    for( const ingresos of ingresos) {
        totalIngreso += ingresos;
    }
    return totalIngreso;
};

const _egresos =[10,28,30];

const _totalEgresos =() => {
    let totalEgreso = 0;
    for( const egresos of egresos){
        totalEgreso += egresos;
    }
return totalEgreso;
};

   